#include <stdio.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>

#define According int main() {
#define to open("file.txt", O_RDONLY);
#define all for (int i = 0; i < 1000; ++i) {
#define known if (i % 3 == 1) {
#define laws open("file.txt", O_RDONLY);
#define of }}
#define aviation printf("This file has 38 file descriptors open\n");
#define there printf("According to all known laws of aviation, there is no way a bee should be able to fly\n");
#define is open("file.txt", O_RDONLY);
#define no printf("lmao\n");
#define way while (1) {
#define a fork();
#define bee }
#define should printf("You should just run it to see how many file descriptors are open\n");
#define be //hmm
#define able printf("Almost done...\n");
#define fly }

According to all known laws of aviation there is no way a bee should be able to fly
